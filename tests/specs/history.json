[
  {
    "name": "/history shows empty state for new user",
    "steps": [
      {
        "send": { "text": "/history" },
        "expect": [
          {
            "method": "sendMessage",
            "payload": {
              "text": "No signals yet. Signals you receive will appear here."
            }
          }
        ]
      }
    ]
  },
  {
    "name": "History menu button shows empty state",
    "steps": [
      {
        "send": { "callback": "history:show" },
        "expect": [
          {
            "method": "editMessageText",
            "payload": {
              "text": "No signals yet. Signals you receive will appear here."
            }
          }
        ]
      }
    ]
  }
]
